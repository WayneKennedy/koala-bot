# software/

Pi 5 / ROS2 high-level brain + tooling (planned - Phase 1). The "cerebrum".

- Perception (camera-eyes), behaviour/mission, LLM personality, networking.
- Sends setpoints to the MCU, reads telemetry - never runs the balance loop.
- Licence: `MIT`.
